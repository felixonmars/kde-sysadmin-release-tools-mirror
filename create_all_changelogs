. versions

mydir=$(dirname $0)

mkdir changelogs
for mod in `cat modules`; do
   set_urls $mod
   svn log -v --stop-on-copy --xml -r768944:780206 $BASE/$HEADURL | python $mydir/svn2log.py  &&
	mv ChangeLog changelogs/$mod.txt
done
